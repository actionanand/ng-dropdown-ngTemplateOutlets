import { AddCssClassHeaderWrapDirective } from './add-css-class-header-wrap.directive';

describe('AddCssClassPopoverWrapDirective', () => {
  it('should create an instance', () => {
    const directive = new AddCssClassHeaderWrapDirective();
    expect(directive).toBeTruthy();
  });
});
